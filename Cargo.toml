[package]
name = "helios-sci"
version = "0.1.0"
edition = "2024"

[dependencies]
cfg-if = "1.0"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = [
  'cfg(target_os, values("helios"))',
] }
